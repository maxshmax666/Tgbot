# Bot
BOT_TOKEN=
MINIAPP_URL=https://tgbot-3cm.pages.dev/
WEBAPP_URL=https://tgbot-3cm.pages.dev/
ADMIN_CHAT_ID=
DB_PATH=./bot.db

# Payments (YooKassa)
YOOKASSA_SHOP_ID=
YOOKASSA_SECRET_KEY=
YOOKASSA_RETURN_URL=

# Email notifications (SMTP)
ADMIN_EMAIL=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_TLS=true

# Cloudflare Pages Functions
# BOT_TOKEN — Telegram bot token (see above; required for Login Widget signature verification)
# GOOGLE_CLIENT_ID — OAuth Client ID (Google Identity Services)
GOOGLE_CLIENT_ID=
# ADMIN_PASSWORD_HASH (bcrypt) - production
# Example: node scripts/hash-admin-password.mjs "your_password"
ADMIN_PASSWORD_HASH=
# ADMIN_PASSWORD - local fallback (plain text, use only locally)
ADMIN_PASSWORD=
JWT_SECRET=change-me
# Optional: prefix for media assets (default empty -> relative)
PUBLIC_MEDIA_BASE_URL=
